export const environment = {
  production: true,
  apiUrl: 'https://api.env-monitor.com/api', // Update with your production API URL
  appVersion: '1.0.0',
  features: {
    enableRealTimeUpdates: true,
    enableNotifications: true,
  },
};
